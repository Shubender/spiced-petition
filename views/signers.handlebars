<div class="signers">
    <h1>List of signers ({{signersCount}}):</h1>
    <ul class="signers-text" style="list-style: none;">
        {{#each allData}}
            <li>{{firstname}} {{lastname}}</li>
        {{/each}}
    </ul>
</div>